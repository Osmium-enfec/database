version: '3.8'

services:
  go-content:
    image: go-content:latest
    container_name: go-content
    hostname: go-content
    restart: unless-stopped
    environment:
      SECRET: ${SECRET}
      STAGE: ${STAGE}
    ports:
      - "8114:8080"
